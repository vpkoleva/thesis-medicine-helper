<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="bg.unisofia.fmi.valentinalatinova.app"
          android:versionCode="1"
          android:versionName="1.0">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:theme="@android:style/Theme.Holo.Light.DarkActionBar"
            android:label="@string/app_name"
            android:icon="@drawable/ic_launcher"
            android:allowBackup="true"
            android:supportsRtl="false">
        <activity
                android:name="MainActivity"
                android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
                android:name="LoginActivity"
                android:label="@string/login_message"/>
        <activity
                android:name="SettingsActivity"
                android:label="@string/settings_menu"/>
        <activity
                android:name="ManageResultActivity"
                android:label="@string/manage_results_window"/>
        <activity android:name="ManageScheduleActivity"/>
        <activity android:name="ManageResultsValueActivity"/>
    </application>

</manifest>
